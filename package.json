{
  "name": "@smsshcss/root",
  "private": true,
  "version": "1.0.1",
  "scripts": {
    "format": "prettier --write .",
    "lint": "prettier --write . && eslint --fix '**/*.{js,jsx,ts,tsx}' --ignore-path .gitignore",
    "lint:check": "prettier --check . && turbo lint",
    "lint:fix": "prettier --write . && eslint --fix",
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "clean": "turbo clean && rm -rf node_modules",
    "type-check": "turbo type-check",
    "prepare": "husky",
    "preinstall": "npx only-allow yarn",
    "clean:all": "yarn clean && rm -rf .turbo .yarn/cache",
    "clean:install": "yarn clean:all && yarn install"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "yarn prettier --write",
      "yarn eslint --fix"
    ],
    "*.{json,md,yml,yaml,css,html}": [
      "yarn prettier --write"
    ]
  },
  "license": "MIT",
  "devDependencies": {
    "@commitlint/cli": "^19.8.0",
    "@commitlint/config-conventional": "^19.8.0",
    "@types/node": "^22.15.23",
    "@typescript-eslint/eslint-plugin": "^8.35.0",
    "@typescript-eslint/parser": "^8.35.0",
    "eslint": "^8.56.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.1",
    "postcss": "^8.4.35",
    "prettier": "^3.0.0",
    "tsup": "^8.0.2",
    "turbo": "^2.5.3",
    "typescript": "^5.8.3",
    "vitest": "^0.34.6"
  },
  "workspaces": [
    "packages/*",
    "packages/@smsshcss/*",
    "playground/*"
  ],
  "volta": {
    "node": "20.18.0",
    "yarn": "4.5.0"
  },
  "packageManager": "yarn@4.5.0"
}
