import smsshcssPlugin from './index.js'

// This is used instead of `export default` to work around a bug in
// `postcss-load-config` and to ensure compatibility with CommonJS environments

// @ts-ignore
module.exports = smsshcssPlugin 